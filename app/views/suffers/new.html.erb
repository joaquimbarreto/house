<h2>New Symptoms</h2>

<%= form_for @suffer do |f| %>

  <%= f.collection_select :patient_id, Patient.all, :id, :name %>

  <%= f.collection_select :symptom_id, Symptom.all, :id, :name %><br>

  <%= f.submit "Diagnosis"%>

<% end %>
